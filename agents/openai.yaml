interface:
  display_name: "Contract–Build–Prove"
  short_description: "Contract, build, audit, and prove substantial work"
  default_prompt: "Use $contract-build-prove to execute this repository task with proportional rigor, a frozen acceptance contract, and independent evidence-backed completion."

policy:
  allow_implicit_invocation: true
